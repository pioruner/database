syntax = "proto3";

package transfer;

// указываем go_package: имя модуля из go.mod + ; + имя пакета
option go_package = "ggithub.com/pioruner/database;proto";


service TransferService {
  rpc Ping (PingRequest) returns (PingResponse);
}

message PingRequest {
  string msg = 1;
}

message PingResponse {
  string msg = 1;
}